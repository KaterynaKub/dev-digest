/** Co-located constants for IntentCard. */

/** confidence <= this shows the low-confidence caveat (constraint: mockup shows
 *  the numeric value only when it is worth calling out). */
export const LOW_CONFIDENCE_THRESHOLD = 0.4;
