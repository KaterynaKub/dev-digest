{
  "name": "Skills list renders seeded skills, previews a body, and the agent editor's Skills tab shows the order hint",
  "description": "Loads /skills and confirms a seeded skill (Test Coverage Rubric) is rendered; clicking its card opens the in-page preview showing the skill body. Then opens the Test Quality Reviewer agent and switches to its Skills tab to confirm the order-hint copy renders. Exercises the Skills list route, SkillCard/SkillPreview, and the agent editor's Skills tab. Read-only seeded data (no model call, no file upload).",
  "steps": [
    { "cmd": ["open", "{BASE}/skills"], "label": "load the skills page" },
    { "cmd": ["wait", "--url", "/skills"], "label": "skills route reached" },
    { "cmd": ["wait", "--load", "networkidle"], "label": "skills fetch settles" },
    { "cmd": ["wait", "--text", "Test Coverage Rubric"], "label": "seeded skill card is visible" },
    { "cmd": ["find", "text", "Test Coverage Rubric", "click"], "label": "open the skill's preview pane" },
    { "cmd": ["wait", "--text", "Skill body (Markdown)"], "label": "preview pane renders the skill body section" },
    { "cmd": ["open", "{BASE}/agents"], "label": "load the agents page" },
    { "cmd": ["wait", "--url", "/agents"], "label": "agents route reached" },
    { "cmd": ["wait", "--load", "networkidle"], "label": "agents fetch settles" },
    { "cmd": ["find", "text", "Test Quality Reviewer", "click"], "label": "open the Test Quality Reviewer agent" },
    { "cmd": ["wait", "--load", "networkidle"], "label": "agent detail data settles" },
    { "cmd": ["find", "role", "button", "click", "--name", "Skills"], "label": "switch to the agent's Skills tab" },
    { "cmd": ["wait", "--url", "tab=skills"], "label": "skills tab is active in the URL" },
    { "cmd": ["wait", "--text", "Order matters"], "label": "the order-hint copy renders" }
  ]
}
